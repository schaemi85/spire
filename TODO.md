# TODO


## IDEAS
- Using anchors slots instead of basic search and replace with text. i.e.: // @slot
- Backstage compatibility: we should be able to generate backstage compatible templates / make the template configurable [[ ]] or {{ }} and transform spire manifest into Backstage manifest
- Install Remote plugins


## TO FIX
